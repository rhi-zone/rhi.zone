# rhi.zone

Static assets for [rhi.zone](https://rhi.zone), served via Cloudflare Pages.

## Install scripts

```bash
# Linux / macOS
curl -fsSL https://rhi.zone/normalize/install.sh | sh

# Windows (PowerShell)
irm https://rhi.zone/normalize/install.ps1 | iex
```
